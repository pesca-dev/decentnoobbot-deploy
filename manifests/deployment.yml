apiVersion: apps/v1
kind: Deployment
metadata:
  name: decentnoobbot-deployment
  namespace: decentnoobbot
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: decentnoobbot-deployment
  strategy: {}
  template:
    metadata:
      labels:
        io.kompose.service: decentnoobbot-deployment
    spec:
      containers:
        - image: ghcr.io/pesca-dev/decentnoobbot:420408984ad05748d620c0a9100db2a9f5af4f0f
          imagePullPolicy: Always
          name: decentnoobbot-deployment
          env:
          - name: RUST_LOG
            value: debug
          - name: BOT_TOKEN
            valueFrom:
              secretKeyRef:
                name: decentnoobbot-secret
                key: bot-token
          resources: {}
      restartPolicy: Always
status: {}
